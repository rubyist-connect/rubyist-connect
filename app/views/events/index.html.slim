h1 Events

= link_to 'new event', new_event_path

table.table.table-hover
  tbody
    tr
    - @events.each do |event|
      tr
        td= link_to event.name, event
        - # TODO: i18n化
        td #{event.users.count} 人
        td= link_to 'Edit', edit_event_path(event)
